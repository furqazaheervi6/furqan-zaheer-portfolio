# tokens:read script refreshes that snapshot from an upstream source.
# Point it elsewhere with TOKENS_READ_SOURCE=<scheme>://<path> (fs:// now, git:// later)
# or a CLI arg. Default upstream is ../../../design-tokens.
# TOKENS_READ_SOURCE=fs:///absolute/path/to/design-tokens
